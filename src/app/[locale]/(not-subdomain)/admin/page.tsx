import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin",
  description: "-",
};

export default function Pages() {
  return (
    <div className="relative flex h-full max-h-screen w-full grow">admin</div>
  );
}
